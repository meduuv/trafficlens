[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "trafficlens"
version = "0.1.0"
description = "Read-only flow record summarization helpers"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]
